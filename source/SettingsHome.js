enyo.kind({
    name: "SettingsHome",
    kind: enyo.Control,

    /* The Components contains the following:

       application settings such as:


     */
    components: [
        {kind: enyo.Control, tag:"p", content: "Unavailable as of yet."}
    ]
});